import { Participant } from '../entities/participant.entity';

export class RegistrationService {
	register(participant: Participant): void {
		void participant;
	}

	updateProfile(participant: Participant): void {
		void participant;
	}
}
